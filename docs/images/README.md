# README Screenshot Assets

Suggested screenshot files referenced by the READMEs:

- `first_run_model_picker.png` — first-run model package selection dialog.
- `config_panel_example.png` — optional config panel overview.

Current in-repo UI image example:
- `doc/src/configpanel.png`
